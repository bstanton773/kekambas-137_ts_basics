// Pure JavaScript - No TypeScript

/**
 * 
 * @param {number} length 
 * @param {number} width 
 * @returns {number}
 */
function squareFootage2(length, width){
    return length * width
}

module.exports = {
    squareFootage2
}