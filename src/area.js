// below is not a comment (once the checkJS is enabled)
// this tells TS not to type check this file

// @ts-nocheck
function squareFootage(length, width){
    return length * width
}

module.exports = {
    squareFootage
}
